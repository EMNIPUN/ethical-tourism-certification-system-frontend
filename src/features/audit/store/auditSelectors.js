export const selectAuditState = (state) => state.audit
export const selectAuditFilter = (state) => state.audit.filter
export const selectAuditPage = (state) => state.audit.page
export const selectAuditLimit = (state) => state.audit.limit

